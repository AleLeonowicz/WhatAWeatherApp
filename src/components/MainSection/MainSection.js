import classes from './MainSection.module.scss';

const MainSection = props => {
  return (
    <div className={classes.mainSection}>
      <div className={classes.mainSection_Card}></div>
    </div>
  );
};

export default MainSection;
